{
  "default": {
    "type": "azure",
    "api_version": "2023-07-01-preview",
    "azure_endpoint": "${env:AZURE_OPENAI_ENDPOINT}",
    "azure_deployment": "${env:AZURE_OPENAI_DEPLOYMENT:gpt-4}",
    "api_key": "${env:AZURE_OPENAI_KEY}"
  }
}